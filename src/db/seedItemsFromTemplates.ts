// src/db/seedItemsFromTemplates.ts
// Items are optional in current Firestore-first builds. This file is kept for compatibility.

export async function ensureItemsFromTemplates(_userId: string): Promise<void> {
  // no-op
}
